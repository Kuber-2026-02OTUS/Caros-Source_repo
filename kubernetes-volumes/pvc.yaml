apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homework-pvc
  namespace: homework
spec:
#  storageClassName: standard
  storageClassName: minikube-hostpath-retain
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi